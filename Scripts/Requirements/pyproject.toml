[tool.ruff]
# Spécifiez les chemins des fichiers ou des répertoires à inclure dans le linting
include = ["/Users/f.b/Desktop/Data_Science/Watches/Scripts/cleaning.py"]

# Spécifiez les chemins des fichiers ou des répertoires à exclure du linting
exclude = ["build", "dist", ".venv"]

# Activer ou désactiver des règles spécifiques
select = ["E", "F", "W"]

# Liste de règles à désactiver
ignore = ["E501"] # Par exemple, ignorer les lignes trop longues
